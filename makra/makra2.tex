
% FIXME: cleanup
\newcommand{\vect}[1]{\boldsymbol{#1}}
\newcommand{\unitvect}[1]{\widetilde{\vect{#1}}}
\newcommand{\vektor}[1]{\vec{#1}}
\newcommand{\fourier}{\mathcal{F}}
\newcommand{\dual}[1]{\widetilde{#1}}

% vektorovy sucin
\newcommand{\cross}{\times}

% rozne funkcie
%\newcommand{\sinc}{{\rm sinc}}

\DeclareMathOperator{\sinc}{sinc}
%\DeclareMathOperator{\Re}{Re}

% skrateny zapis lim x->inf
\newcommand{\limtoinf}[1]{\lim_{#1 \imply \infty}}

%zapis int -pi pi
\newcommand{\intpipi}{\int_{-\pi}^{\pi}}
\newcommand{\intLL}{\int_{-L}^{L}}
\newcommand{\intinfinf}{\int_{-\infty}^{\infty}}

% dx za integralom
\newcommand{\dd}{\; \mathrm{d}}

%
\def\todo#1{{\bf {\color{red} TODO:}#1}}
\def\todo#1{}
\def\smalltodo#1{}

\def\fixme#1{{\bf {\color{red} FIXME:}#1}}
\def\fixme#1{}
% ->
\def\sipka{\rightarrow}
\def\imply{\sipka}

% <=>
\newcommand{\equivalence}{\Leftrightarrow}

\def\suminf#1{\sum_{#1=-\infty}^{\infty}}
\def\sumtoinf#1{\sum_{#1=1}^{\infty}}
% imaginarna jednotka
\newcommand{\imag}{\hat{\imath}}

%jednotky
\def\cm{\,\mbox{cm}}

% obory hodnot
\def\Z{\mathbb{Z}}
\def\N{\mathbb{N}}
\def\Nz{\mathbb{N}_0}
\def\R{\mathbb{R}}
\def\C{\mathbb{C}}

%\def\Re{\mbox{Re}}

\def\Cont{\mbox{C}}
%PC
\def\PC{\mbox{PC}}
\def\PCab{\PC[a,b]}
\def\PCinf{\PC(-\infty,\infty)}

\def\PS{\mbox{PS}}
\def\PSab{\mbox{PS}[a,b]}
\def\PSinf{\PS(-\infty,\infty)}
% L2
\def\LL{\mbox{L}^2}
\def\LLab{\LL(a,b)}
\def\LLinf{\LL(-\infty,\infty)}

% norm
\def\norm#1{\left\lVert#1\right\rVert}
%inner product
\newcommand{\inner}[2]{\left\langle #1,#2 \right\rangle}
%inner product complex
\def\innerc#1#2{\left\langle #1,#2 \right\rangle}

\def\conjug#1{\overline{#1}}

\def\floor#1{\lfloor #1 \rfloor}

% boolean and,or
\def\land{\wedge}
\def\lor{\vee}

% set union, intersection
\def\union{\cup}
\def\intersect{\cap}

\newcommand{\sgn}{\operatorname{sgn}}

%FIXME: need mainternance

\newcommand{\gv}[1]{\ensuremath{\mbox{\boldmath$ #1 $}}} 
% for vectors of Greek letters
%\newcommand{\uv}[1]{\ensuremath{\mathbf{\hat{#1}}}} % for unit vector
\newcommand{\abs}[1]{\left| #1 \right|} % for absolute value
\newcommand{\avg}[1]{\left< #1 \right>} % for average
\let\underdot=\d % rename builtin command \d{} to \underdot{}
\renewcommand{\d}[2]{\frac{d #1}{d #2}} % for derivatives
%\newcommand{\dd}[2]{\frac{d^2 #1}{d #2^2}} % for double derivatives
\newcommand{\pd}[2]{\frac{\partial #1}{\partial #2}} 
% for partial derivatives
\newcommand{\pdd}[2]{\frac{\partial^2 #1}{\partial #2^2}} 
% for double partial derivatives
\newcommand{\matrixel}[3]{\left< #1 \vphantom{#2#3} \right|
 #2 \left| #3 \vphantom{#1#2} \right>} % for Dirac matrix elements
\newcommand{\grad}[1]{\gv{\nabla} #1} % for gradient
\let\divsymb=\div % rename builtin command \div to \divsymb
\renewcommand{\div}[1]{\gv{\nabla} \cdot #1} % for divergence
\newcommand{\curl}[1]{\gv{\nabla} \times #1} % for curl

%%%%%%%%%%% MATICKE MAKRA

\font\tenrm=csr10

\def\eps{\varepsilon}
\def\R{{\mathbb R}}
\def\lvec#1{\overrightarrow{#1}}
\def\uhol{{\measuredangle}}
\def\then{\Rightarrow}
\def\lg{\log_2}
\def\div{{\rm div}}

%dvojverzia

%to comment out one version use:
%\let\remark=\comment 
%\let\endremark=\endcomment
\newenvironment{formal}{{\color{blue} FORMAL:}}{}
\newenvironment{funny}{{\color{blue} FUNNY:}}{}
